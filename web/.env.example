# OIDC Configuration (required for SSO login)
# Copy this file to .env.local and fill in your values

# OIDC provider URL (issuer)
VITE_OIDC_AUTHORITY=https://your-oidc-provider.com

# Client ID registered with the OIDC provider
VITE_OIDC_CLIENT_ID=your-client-id

# Redirect URI for the callback (must match OIDC provider config)
# Defaults to {origin}/auth/callback if not set
# VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback

# Post-logout redirect URI
# Defaults to origin if not set
# VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# OAuth scopes (space-separated)
# Defaults to "openid profile email offline_access" if not set
# VITE_OIDC_SCOPE=openid profile email offline_access
